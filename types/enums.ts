export enum AuthState {
  SIGNIN = "Sign In",
  SIGNUP = "Sign Up",
}

export enum TabType {
  USERS = "users",
  GROUPS = "groups",
}

export enum MessageType {
  TEXT = "text",
  IMAGE = "image",
  AUDIO = "audio",
}
